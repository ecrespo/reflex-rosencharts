"""Namespace dir for the reflex-rosencharts package source (see [tool.setuptools.packages.find] where).

Not packaged: setuptools scans inside this directory; this file only makes the
on-disk path importable for Reflex's .pyi build step.
"""
